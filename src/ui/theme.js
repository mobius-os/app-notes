// DEPRECATED — T() and cssVar() are the banned inline-style pattern.
// Styling now lives in src/ui/css.js (one const CSS, injected once as
// <style>{CSS}</style> at the nt-root level). Do not re-introduce T() callers.
